version: '3'
services:
  db:
    image: postgres
    container_name: rest-api-financial-management-db
    environment:
      - POSTGRES_USER=rest-api-financial-management
      - POSTGRES_PASSWORD=password
    volumes:
      - ./tmp/db:/var/lib/postgresql/data
  api:
    build: .
    container_name: rest-api-financial-management-api
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    depends_on:
      - db